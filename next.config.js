/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/ai_project',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

export default nextConfig
